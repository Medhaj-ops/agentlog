[project]
name = "agentlog-ui"
version = "0.1.0a0"
description = "Trace viewer UI for agentlog"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
